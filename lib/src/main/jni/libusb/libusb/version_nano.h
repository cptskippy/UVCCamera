/**
 * \brief Define libusb nano version number.
 *
 * Header providing the nano version component used by version.h for
 * complete version identification.
 *
 * Exports:
 *   LIBUSB_NANO: Nano version integer
 *
 * Dependencies:
 *   None
 *
 * Architecture Note:
 *   Updated by build system during release tagging.
 */
#define LIBUSB_NANO 10903
