/// Type of UVC camera device event.
///
/// Values:
///   attached: Device physically attached.
///   detached: Device physically detached.
///   connected: Device connected to system.
///   disconnected: Device disconnected from system.
enum UvcCameraDeviceEventType { attached, detached, connected, disconnected }
